<%= form_for(@dependency) do |f| %>
  <% if @dependency.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dependency.errors.count, "error") %> prohibited this dependency from being saved:</h2>

      <ul>
      <% @dependency.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descripcion %><br />
    <%= f.text_field :descripcion %>
  </div>
  <div class="actions">
    <%= link_to 'Regregar', dependencies_path, :class=>"btn" %>
    <%= f.submit   :class=>"btn btn-primary" %>
    
  </div>
<% end %>
